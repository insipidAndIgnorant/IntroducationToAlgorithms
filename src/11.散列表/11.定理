11.1
  在简单均匀散列的假设下，对于用链接法解决冲突的散列表，一次不成功的查找的平均时间为Om(1+a)
  // a装载因子 a = n/m

11.2
  在简单均匀散列的假设下，对于用链接法解决冲突的散列表，一次成功的查找的平均时间为Om(1+a)

11.3
  如果h选自一组全域散列函数，将n个关键字散列到一个大小为m的散列表T中，并用链接法解决冲突。如果关键字k不在表中，
  则k被散列到其中的链表的期望长度E(nh(k))至多为a=n/m。如果关键字k在表中，则包含关键字的链表的期望长度E(nh(k))至多为1+a

11.4
  对于一个具有m个槽且初始时为空的表，利用全域散列法和链接法解决冲突，需要Om(n)的期望时间来处理任何包含了n个INSERT、SEARCH
  和DELETE的操作序列，其中该序列包含了O(m)个INSERT操作

11.5
  由公式11.3、11.4定义的散列函数簇是全域的
  // 11.3 hab(k) = ((ak+b) mod p) mod m
  // 11.4 散列函数簇 = {hab: a属于Z*p, b属于Zp}

11.6
  给定一个状态因子为a=n/m<1的开放寻址散列表，并假设是均匀散列的，则对于一次不成功的查找，其期望的探查次数至多为1/(1-a)

11.7
  假设采用的是均匀散列，平均情况下，向一个装载因子为a的开放寻址散列表中插入一个元素至多需要做1/(1-a)次探查

11.8
  对于一个装载因子为a<1的开放寻址散列表，一次成功查找中的探查期望数至多为 (1/a)ln(1/(1-a))

11.9
  如果从一个全域散列函类中随机选出散列函数h,将n个关键字存储在一个大小为m=n^2的散列表中，那么表中出现冲突的概率小于1/2

11.10
  如果从一个全域散列函类中随机选出散列函数h,将n个关键字存储在一个大小为m=n的散列表中，那么
        E[∑j=0~m-1 {nj ^2}] < 2n
        // nj为散列到槽j的关键字数

11.11
  如果从一个全域散列函类中随机选出散列函数h,将n个关键字存储在一个大小为m=n的散列表中，并且将每个二次散列表大小设为
  mj = nj^2(j=0~m-1),则在一个完全散列方案中，存储所有二次散列表所需的总量的期望小于2n


11.12
  如果从一个全域散列函类中随机选出散列函数h,将n个关键字存储在一个大小为m=n的散列表中，并且将每个二次散列表大小设为
  mj = nj^2(j=0~m-1),则用于存储所有二级散列表的存储总量的期望大于或等于4n的概率小于1/2

11.13